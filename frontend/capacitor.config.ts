import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'C:\\ionic-temp\\encounter-daily',
  webDir: 'www'
};

export default config;
